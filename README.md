# SciTune
A Discord music bot with many features. (Still in development)

## Development
* Clone project
* Rename `config-demo.json` to `config.json`
* Create a discord bot following their guide and add your token to `config.json`
* Run `npm install`
* Start bot with `node index.js`
